---
// Entrée interne PRINCIPALE du cockpit. noindex, hors navigation publique.
// Le contenu vit dans un composant partagé (réutilisé par /cockpit-roadmap).
import Base from '../layouts/Base.astro';
import CockpitDashboard from '../components/CockpitDashboard.astro';
---
<Base
  title="Cockpit artiste — centre opérationnel"
  description="Cockpit interne (préparer · publier · protéger · organiser) — outil non public."
  noindex={true}
>
  <CockpitDashboard />
</Base>
